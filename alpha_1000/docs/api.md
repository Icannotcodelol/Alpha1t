# API Reference

The Alpha-1000 package exposes the following primary modules:

- `engine` for game logic and orchestration.
- `bots` for baseline strategies and tournaments.
- `ui` for the Streamlit interface.
- `rl` for training agents.
